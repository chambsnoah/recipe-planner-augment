# Public Supabase configuration for local development
# Do NOT store the Supabase service role key here or in any client-exposed file.
# Use the service role key only on server-side environments (serverless functions, backend).

NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_public_key

# Optional (server-side only):
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
